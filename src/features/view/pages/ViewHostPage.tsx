import { FC, memo, useEffect } from 'react';
import { AppLoadingSpinner } from 'src/components/AppLoadingSpinner';

import style from "./ViewHostPage.module.css";

const ViewHostPageComponent: FC = () => {

//   if (isLoading) {
//     return <AppLoadingSpinner />
//   }

//   if (profile == null) {
//     return <h1>User not found</h1>
//   }

  return (
    <div>

    </div>
  )
}

export const ViewHostPage = memo(ViewHostPageComponent);
