import { FC, memo, useEffect } from 'react';
import { AppLoadingSpinner } from 'src/components/AppLoadingSpinner';

import style from "./ViewMemberPage.module.css";

const ViewMemberPageComponent: FC = () => {

//   if (isLoading) {
//     return <AppLoadingSpinner />
//   }

//   if (profile == null) {
//     return <h1>User not found</h1>
//   }

  return (
    <div>

    </div>
  )
}

export const ViewMemberPage = memo(ViewMemberPageComponent);
